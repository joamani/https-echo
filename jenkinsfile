pipeline {
    agent any
    
    environment {
        DOCKER_IMAGE_NAME = 'myapp'
        DOCKER_REGISTRY = 'docker.io' // Replace with your Docker registry URL
        DOCKER_REGISTRY_CREDENTIAL_ID = 'dev' // Replace with your Jenkins credentials ID for Docker registry authentication
    }
    
    stages {
        stage('Build Go App') {
            steps {
                // Checkout source code from your repository
                git 'https://github.com/joamani/https-echo.git'
                
                // Build Go binary
                sh 'go build -o app ./'
            }
        }
        
        stage('Build Docker Image') {
            steps {
                // Build Docker image
                script {
                    docker.build("${DOCKER_REGISTRY}/${DOCKER_IMAGE_NAME}:latest", '.')
                }
            }
        }
        
        stage('Push Docker Image') {
            steps {
                // Push Docker image to registry
                script {
                    docker.withRegistry("docker.io", "dev") {
                        docker.image("${DOCKER_REGISTRY}/${DOCKER_IMAGE_NAME}:latest").push()
                    }
                }
            }
        }
    }
}
